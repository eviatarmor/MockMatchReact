/** Shared storage name so docs + marketing share one decision. */
export const SITE_PREFS_COOKIE_NAME = "mm_site_prefs"

export const SITE_PREFS_VERSION = 1 as const

/** ~1 year */
export const DEFAULT_PREFS_MAX_AGE_SECONDS = 60 * 60 * 24 * 365
