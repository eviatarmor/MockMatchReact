# `@mockmatch/collab`

Product-agnostic document collaboration client:

- WebSocket room hook (`useCollabRoom`) — host injects ticket fetch
- Surface / cursor helpers
- Presence stack, remote cursors, room-full gate
- Path-op apply + permission helpers

Share admin UI (credits, tRPC mutations, product copy) stays in the host app.
