# `@mockmatch/whiteboard`

Product-agnostic infinite whiteboard shell:

- Pan/zoom canvas (host supplies chrome)
- Tools: select, sticky, text, shapes, pen, connector
- Document model (id → element map) + commands + undo stack
- Templates gallery data + presentational panel

Host supplies i18n labels, persistence, collab (Yjs/share), and practice chrome.
