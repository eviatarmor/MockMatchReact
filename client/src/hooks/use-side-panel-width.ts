/**
 * Re-export shared hook — keep `@/hooks/use-side-panel-width` stable for
 * existing features; new code may import `@mockmatch/ui/hooks/use-side-panel-width`.
 */
export { useSidePanelWidth } from "@mockmatch/ui/hooks/use-side-panel-width"
