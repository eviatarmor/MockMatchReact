/** @deprecated Import from `@mockmatch/document-editor` — re-export for gradual migration. */
export {
  lintText,
  languageToDialect,
  Dialect,
  type GrammarIssue,
} from "@mockmatch/document-editor"
