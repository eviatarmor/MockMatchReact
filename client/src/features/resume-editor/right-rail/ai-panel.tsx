import { AssistantPanel } from "@/features/document-assistant"

/** Editor AI rail body — chat assistant (shared document-assistant shell). */
export function AiPanel() {
  return <AssistantPanel />
}
